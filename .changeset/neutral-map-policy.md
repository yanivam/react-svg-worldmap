---
"react-svg-worldmap": patch
---

Document a neutral map data policy for the bundled world map, add a maintainer-facing overrides register for sensitive geopolitical cases, and preserve the package README during npm publishing so the policy appears on npm.
