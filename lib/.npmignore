src
tsconfig.json
*.tgz

# Source maps are useful for local development but add ~862 KB to the
# published package without benefiting consumers' bundlers or DevTools.
dist/*.map

# Build tooling — never shipped
tsup.config.ts
vitest.config.ts
scripts/
