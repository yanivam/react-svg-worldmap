# @react-svg-worldmap/regions

Optional starter region data and provider helpers for `react-svg-worldmap`.

The core world map package does not require this package. Add it only when an application needs reviewed country-scoped region detail.
